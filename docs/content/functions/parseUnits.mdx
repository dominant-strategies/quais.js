---
title: parseUnits
icon: square-f
iconType: solid
---

```ts
function parseUnits(value, unit?): bigint
```

Converts the //decimal string// `value` to a BigInt, assuming `unit` decimal places. The `unit` may the number of
decimal places or the name of a unit (e.g. `"gwei"` for 9 decimal places).

## Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `value` | `string` | The value to convert. |
| `unit`? | `string` \| [`Numeric`](/sdk/content/type-aliases/Numeric) | The unit to convert from. Default is `18` |

## Returns

`bigint`

The converted value.

## Throws

If the unit is invalid.

## Throws

If the value is not a string.

## Source

[utils/units.ts:58](https://github.com/dominant-strategies/quais-6.js/blob/e281b3ca939b91260332b67ef4ba6e8b0a3d4134/src/utils/units.ts#L58)
