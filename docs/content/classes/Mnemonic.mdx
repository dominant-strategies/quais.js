---
title: Mnemonic
icon: square-c
iconType: solid
---

A **Mnemonic** wraps all properties required to compute [BIP-39](https://en.bitcoin.it/wiki/BIP_0039) seeds and
convert between phrases and entropy.

## Constructors

### new Mnemonic()

```ts
new Mnemonic(
   guard, 
   entropy, 
   phrase, 
   password?, 
   wordlist?): Mnemonic
```

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `guard` | `any` | The guard object. |
| `entropy` | `string` | The entropy. |
| `phrase` | `string` | The mnemonic phrase. |
| `password`? | `null` \| `string` | The password for the mnemonic. |
| `wordlist`? | `null` \| [`Wordlist`](/sdk/content/classes/Wordlist) | The wordlist for the mnemonic. |

#### Returns

[`Mnemonic`](/sdk/content/classes/Mnemonic)

#### Source

[wallet/mnemonic.ts:182](https://github.com/dominant-strategies/quais-6.js/blob/e281b3ca939b91260332b67ef4ba6e8b0a3d4134/src/wallet/mnemonic.ts#L182)

## Properties

| Property | Modifier | Type | Description |
| :------ | :------ | :------ | :------ |
| `entropy` | `readonly` | `string` | The underlying entropy which the mnemonic encodes. |
| `password` | `readonly` | `string` | The password used for this mnemonic. If no password is used this is the empty string (i.e. `""`) as per the<br />specification. |
| `phrase` | `readonly` | `string` | The mnemonic phrase of 12, 15, 18, 21 or 24 words.<br /><br />Use the [**wordlist**](/sdk/content/classes/Mnemonic) `split` method to get the individual words. |
| `wordlist` | `readonly` | [`Wordlist`](/sdk/content/classes/Wordlist) | The wordlist for this mnemonic. |

## Methods

### computeSeed()

```ts
computeSeed(): string
```

Returns the seed for the mnemonic.

#### Returns

`string`

The seed.

#### Source

[wallet/mnemonic.ts:198](https://github.com/dominant-strategies/quais-6.js/blob/e281b3ca939b91260332b67ef4ba6e8b0a3d4134/src/wallet/mnemonic.ts#L198)

***

### entropyToPhrase()

```ts
static entropyToPhrase(_entropy, wordlist?): string
```

Returns the phrase for `mnemonic`.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `_entropy` | [`BytesLike`](/sdk/content/type-aliases/BytesLike) | The entropy for the mnemonic. |
| `wordlist`? | `null` \| [`Wordlist`](/sdk/content/classes/Wordlist) | The wordlist for the mnemonic. |

#### Returns

`string`

The mnemonic phrase.

#### Source

[wallet/mnemonic.ts:255](https://github.com/dominant-strategies/quais-6.js/blob/e281b3ca939b91260332b67ef4ba6e8b0a3d4134/src/wallet/mnemonic.ts#L255)

***

### fromEntropy()

```ts
static fromEntropy(
   _entropy, 
   password?, 
   wordlist?): Mnemonic
```

Create a new **Mnemonic** from the `entropy`.

The default `password` is the empty string and the default wordlist is the [[**English wordlist**](/sdk/content/classes/LangEn).

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `_entropy` | [`BytesLike`](/sdk/content/type-aliases/BytesLike) | The entropy for the mnemonic. |
| `password`? | `null` \| `string` | The password for the mnemonic. |
| `wordlist`? | `null` \| [`Wordlist`](/sdk/content/classes/Wordlist) | The wordlist for the mnemonic. |

#### Returns

[`Mnemonic`](/sdk/content/classes/Mnemonic)

The new Mnemonic object.

#### Source

[wallet/mnemonic.ts:236](https://github.com/dominant-strategies/quais-6.js/blob/e281b3ca939b91260332b67ef4ba6e8b0a3d4134/src/wallet/mnemonic.ts#L236)

***

### fromPhrase()

```ts
static fromPhrase(
   phrase, 
   password?, 
   wordlist?): Mnemonic
```

Creates a new Mnemonic for the `phrase`.

The default `password` is the empty string and the default wordlist is the [**English wordlist**](/sdk/content/classes/LangEn).

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `phrase` | `string` | The mnemonic phrase. |
| `password`? | `null` \| `string` | The password for the mnemonic. |
| `wordlist`? | `null` \| [`Wordlist`](/sdk/content/classes/Wordlist) | The wordlist for the mnemonic. |

#### Returns

[`Mnemonic`](/sdk/content/classes/Mnemonic)

The new Mnemonic object.

#### Source

[wallet/mnemonic.ts:213](https://github.com/dominant-strategies/quais-6.js/blob/e281b3ca939b91260332b67ef4ba6e8b0a3d4134/src/wallet/mnemonic.ts#L213)

***

### isValidMnemonic()

```ts
static isValidMnemonic(phrase, wordlist?): boolean
```

Returns true if `phrase` is a valid [BIP-39](https://en.bitcoin.it/wiki/BIP_0039) phrase.

This checks all the provided words belong to the `wordlist`, that the length is valid and the checksum is
correct.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `phrase` | `string` | The mnemonic phrase. |
| `wordlist`? | `null` \| [`Wordlist`](/sdk/content/classes/Wordlist) | The wordlist for the mnemonic. |

#### Returns

`boolean`

True if the phrase is valid.

#### Throws

If the phrase is invalid.

#### Source

[wallet/mnemonic.ts:282](https://github.com/dominant-strategies/quais-6.js/blob/e281b3ca939b91260332b67ef4ba6e8b0a3d4134/src/wallet/mnemonic.ts#L282)

***

### phraseToEntropy()

```ts
static phraseToEntropy(phrase, wordlist?): string
```

Returns the entropy for `phrase`.

#### Parameters

| Parameter | Type | Description |
| :------ | :------ | :------ |
| `phrase` | `string` | The mnemonic phrase. |
| `wordlist`? | `null` \| [`Wordlist`](/sdk/content/classes/Wordlist) | The wordlist for the mnemonic. |

#### Returns

`string`

The entropy.

#### Source

[wallet/mnemonic.ts:267](https://github.com/dominant-strategies/quais-6.js/blob/e281b3ca939b91260332b67ef4ba6e8b0a3d4134/src/wallet/mnemonic.ts#L267)
